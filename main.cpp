#include <SFML/Window.hpp>
#include <SFML/Graphics.hpp>
#include <SFML/System.hpp>

int main() {
    sf::Window window(sf::VideoMode({300,200}), "learning SFML",sf::Style::Default);
    while (window.isOpen()) {

    }
    return 0;
}